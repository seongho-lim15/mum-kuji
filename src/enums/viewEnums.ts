export enum ViewEnums {
  LIST = "list",
  CHART = "chart",
  CALENDAR = "calendar",
}

export enum TimeEnums {
  DAY = "day",
  WEEK = "week",
  MONTH = "month",
  MONTHLY = "monthly",
  YEAR = "year",
}
